/**
 * @loquia/worker — SCAFFOLDING ONLY (Milestone 1).
 *
 * The real processing worker (queue consumer, media pipeline, diarization,
 * AI Pack generation) is Milestone 2. In Milestone 1 the pipeline is simulated
 * entirely by the MockAdapter inside apps/web. Nothing runs here.
 */
export const WORKER_STATUS = 'not-implemented-milestone-1' as const;
